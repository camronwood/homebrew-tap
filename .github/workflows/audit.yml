name: Audit casks

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  audit:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Audit and style casks
        run: |
          brew audit --cask Casks/*
          brew style --cask Casks/*
